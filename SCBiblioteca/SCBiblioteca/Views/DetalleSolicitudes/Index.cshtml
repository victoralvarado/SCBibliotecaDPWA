@model IEnumerable<SCBiblioteca.Models.DetalleSolicitud>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Libro.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Solicitud.IdSolicitud)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Libro.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Solicitud.IdSolicitud)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdDetalleSolicitud }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdDetalleSolicitud }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdDetalleSolicitud })
        </td>
    </tr>
}

</table>
