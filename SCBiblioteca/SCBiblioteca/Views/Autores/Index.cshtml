@model IEnumerable<SCBiblioteca.Models.Autor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Autor1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Autor1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAutor }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAutor }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAutor })
        </td>
    </tr>
}

</table>
